{
  "name": "readhelper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "set-webhook": "node --env-file=.env.local scripts/set-webhook.mjs",
    "set-commands": "node --env-file=.env.local scripts/set-commands.mjs",
    "migrate": "node --env-file=.env.local scripts/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.1"
  }
}
